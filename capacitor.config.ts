import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.crz.ricomtr',
  appName: 'ricomtr',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
